VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# NUNCA adicione a service_role key aqui com prefixo VITE_ — ela seria
# embutida no bundle do navegador. Operações privilegiadas devem usar
# Edge Functions (ver supabase/functions/), que recebem a service_role
# automaticamente via variáveis de ambiente do runtime da function.
